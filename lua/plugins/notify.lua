return {
  "rcarriga/nvim-notify",
  opts = {
    -- To prevent "Highlight group 'NotifyBackground' has no background highlight"
    -- error when using catppuccin theme.
    background_colour = "#000000",
  },
}
